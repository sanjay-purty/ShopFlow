services:
  - type: web
    name: shopflow
    env: node
    plan: free
    rootDirectory: backend/
    buildCommand: npm install or yarn install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 20.10.0
